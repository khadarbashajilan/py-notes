[project]
name = "03-dependency-injection"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "asyncio>=4.0.0",
    "fastapi[standard]>=0.139.0",
    "requests>=2.34.2",
]

[tool.uv.workspace]
members = [
    "refactored_product_api",
]
